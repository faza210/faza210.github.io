# faza210.github.io

Static mirror of my personal portfolio. **Canonical site: [emirfaza.com](https://emirfaza.com)** — this repo exists purely as a fallback host (GitHub Pages) in case the primary domain is unreachable from a given network.

Vanilla HTML/CSS/JS. No frameworks, no build step. Served from the repository root via GitHub Pages.
